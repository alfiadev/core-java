public class Array {
    public static void main(String[] args) {

        /*
         * int arr[] = { 2, 2, 2, 44, 44, 3, 3, 3, 3, 23 };
         * for (int i = 0; i < arr.length - 1; i++) {
         * if (arr[i] != arr[i + 1]) {
         * System.out.println(arr[i]);
         * 
         * } else {
         * 
         * System.out.println(arr[arr.length - 1]);
         * }
         * }
         */
        String str = "NewYork";
        System.out.println(str.length());
    }
}
